export default function Registration() {
  return;
}

// -Registration
//     -submit username and password
//     -If successful (no one with same username)
//      then display success message
//     -If unsuccessful, displays error message
